import { Vector } from "./position";
// tslint:disable-next-line:no-magic-numbers
export const SKYBOX_MIN: Vector = { x:-1000, y:-1000, z:-500 };
// tslint:disable-next-line:no-magic-numbers
export const SKYBOX_MAX: Vector = { x:1000, y:1000, z:-5000 };
