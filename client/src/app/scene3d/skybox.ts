import * as THREE from "three";

// tslint:disable-next-line:no-magic-numbers
export const SKYBOX_MIN: THREE.Vector3 = new THREE.Vector3(-1000, -1000, -500);
// tslint:disable-next-line:no-magic-numbers
export const SKYBOX_MAX: THREE.Vector3 = new THREE.Vector3(1000, 1000, -5000);
